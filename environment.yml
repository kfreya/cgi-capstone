name: cgi-capstone
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - openpyxl
      - numpy
      - scikit-learn
      - python-dotenv
      - openai
      - streamlit
      - plotly
      - pypdf
      - pytest
      - ipykernel
      - jupyter
      # Optional Azure/Chroma backend smoke-test dependency:
      # - chromadb
